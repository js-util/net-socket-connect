{
	"name": "@js-util/net-socket-connect",
	"version": "1.0.1",
	"description": "Given the remote port/host, setup a low level, net.Socket. With callbacks and connect timeout!",

	"keywords": [
		"connect",
		"net.Socket",
		"callback",
		"timeout"
	],

	"main": "netSocketConnect.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},

	"author": "picocreator",
	"license": "MIT",
	"private": false,

	"repository": {
		"type": "git",
		"url": "https://github.com/js-util/net-socket-connect.git"
	},
	
	"engines": {
		"node": ">=8.0.0"
	}
}
